apiVersion: apps/v1
kind: Deployment
metadata:
  name: saludplus-deployment
  namespace: saludplus-ns
  labels:
    app: saludplus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: saludplus
  template:
    metadata:
      labels:
        app: saludplus
    spec:
      containers:
      - name: saludplus-app
        image: jose212019/saludplus-app:latest
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10