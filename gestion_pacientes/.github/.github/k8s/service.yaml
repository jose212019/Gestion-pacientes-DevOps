apiVersion: v1
kind: Service
metadata:
  name: saludplus-service
  namespace: saludplus-ns
spec:
  selector:
    app: saludplus
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP